# 当前执行步骤

## Step 1：静态 BuildArea 页面

已完成。

当前脚本：

```text
assets/scripts/core/P0BuildAreaBootstrap.ts
```

挂载方式：

```text
Canvas 添加组件 P0BuildAreaBootstrap
```

已完成内容：

```text
顶部：包子 20、第1波、准备阶段
中间：S形路线、白色可放格、红色锁定格
底部：营、5个缓存格、征兵按钮、开始战斗按钮
```

颜色含义：

```text
白格 = 可放兵
红格 = 未解锁，不能放兵
粉格 = 敌人路线
缓存格 = 征兵后单位先进入这里
```

## Step 2：RecruitSystem 最小征兵

已完成。

点击征兵：

```text
包子 -10
随机生成 刀 / 弓 / 枪 / 骑
生成单位进入第一个空缓存格
单位用颜色方块表示
单位上印关键字和 Lv1
```

单位颜色：

```text
刀 = 红色方块
弓 = 蓝色方块
枪 = 绿色方块
骑 = 黄色方块
```

限制：

```text
缓存区最多 5 个单位
包子不足不能征兵
缓存满不能征兵
```

本步不做：

```text
拖拽
交换
合成
战斗
美术资源
特效资源
```

验收标准：

```text
点击征兵后，包子从20变10
缓存区出现一个随机单位
再次点击后，包子从10变0
缓存区出现第二个随机单位
第三次点击提示包子不足
```

## 下一步

```text
Step 3：Drag
缓存区单位拖到白色布阵格
```

## Step 3：Drag 最小拖拽

已完成。

当前支持：

```text
缓存区单位
↓
拖到白色布阵格
```

放置规则：

```text
只能放到白格
不能放到红格
不能放到路线
不能放到已经有单位的白格
拖错位置会返回原位
```

本步不做：

```text
白格到白格交换
相同单位合成
战斗状态禁止拖拽
```

验收标准：

```text
点击征兵生成单位
拖动单位到白格
单位停在白格上
拖到红格或路线会返回缓存格
```

## 下一步

```text
Step 4：Swap
白格单位之间可以交换位置
```

## Step 4：Swap 最小交换

已完成。

当前支持：

```text
单位A在白格
单位B在白格
拖动单位A到单位B所在白格
A和B交换位置
```

规则：

```text
只能在白格之间交换
拖到红格会返回原位
拖到路线会返回原位
拖到空白白格会移动过去
拖到有单位的白格会交换
```

本步不做：

```text
合成
战斗中禁止拖拽
缓存区单位之间交换
```

验收标准：

```text
先征兵两个单位
分别拖到两个白格
把第一个单位拖到第二个单位所在白格
两个单位互换位置
```

## 下一步

```text
Step 5：Merge
相同单位、相同等级拖到一起时合成
```

## Step 5：Merge 最小合成

已完成。

当前规则：

```text
相同单位 + 相同等级 = 合成
不同单位 = 交换
同单位不同等级 = 交换
```

示例：

```text
刀 Lv1
拖到
刀 Lv1
↓
目标格变成 刀 Lv2
拖拽来源单位消失
```

为了方便测试，当前初始包子临时改为：

```text
50
```

这样最多可以征兵 5 次，更容易出现相同单位。

本步不做：

```text
合成特效
最高等级限制
自动整理
战斗数值变化
```

验收标准：

```text
连续征兵多个单位
把两个相同单位拖到一起
只留下一个单位
等级从 Lv1 变成 Lv2
不同单位拖到一起仍然交换
```

## 下一步

```text
Step 6：Start Battle
点击开始战斗后进入战斗状态，禁止征兵和拖拽
```

## Step 6：Enemy Move 最小敌人移动

已完成。

当前规则：

```text
点击开始战斗
生成黑色敌人方块“兵”
敌人沿粉色路线移动
到达终点后消失
```

重要规则调整：

```text
战斗中仍然可以征兵
战斗中仍然可以拖拽
战斗中仍然可以交换
战斗中仍然可以合成
```

再次点击开始战斗：

```text
追加生成一个敌人
```

本步不做：

```text
单位攻击
敌人血量
生命扣除
金币奖励
下一波
```

验收标准：

```text
点击开始战斗
出现黑色方块“兵”
敌人沿路线移动
敌人到终点消失
移动过程中仍然可以征兵和拖拽单位
```

## 下一步

```text
Step 7：Auto Attack
白格上的单位自动攻击路径上的敌人
```

## Step 7：Auto Attack 最小自动攻击

已完成。

当前规则：

```text
白格上的单位会自动寻找附近敌人
敌人进入攻击范围后自动扣血
敌人血量显示在黑色方块下方
敌人 HP <= 0 后消失
击杀敌人后包子 +5
```

当前数值：

```text
敌人 HP = 20
单位攻击范围 = 170
单位攻击间隔 = 0.8秒
单位伤害 = 4 × 等级
击杀奖励 = 包子 +5
```

本步不做：

```text
弹道
攻击特效
不同兵种差异
生命扣除
下一波自动推进
```

验收标准：

```text
征兵
拖到白格
点击开始战斗
敌人靠近单位后 HP 数字下降
HP 归零后敌人消失
包子数量增加 5
战斗过程中仍然可以征兵、拖拽、合成
```

## 下一步

```text
Step 8：Reward + Next Wave
敌人全清后自动进入下一波，并继续奖励包子
```

## Step 8：Reward + Next Wave 最小波次循环

已完成。

当前规则：

```text
点击开始战斗
进入第1波
第N波生成 2 + N 个敌人
敌人按 0.75 秒间隔生成
全部敌人被击杀或走到终点后，本波结束
本波结束奖励包子 +10
自动进入下一波
```

当前仍然允许：

```text
战斗中征兵
战斗中拖拽
战斗中交换
战斗中合成
```

奖励：

```text
击杀单个敌人：包子 +5
清空一波：包子 +10
```

本步不做：

```text
生命系统
失败条件
20波数值平衡
不同敌人类型
```

验收标准：

```text
点击开始战斗
第1波连续生成3个敌人
击杀敌人获得包子
一波结束后自动进入第2波
第2波生成4个敌人
期间仍然可以征兵、拖拽、合成
```

## 下一步

```text
Step 9：Life / Fail
敌人到达终点扣生命，生命为0时本局失败
```

## Step 9：Life / Fail 最小失败条件

已完成。

当前规则：

```text
玩家初始生命 = 3
顶部显示当前生命
敌人到达终点后消失
每个漏到终点的敌人使生命 -1
生命归零后本局失败
```

失败后：

```text
停止刷怪
清空场上敌人
停止自动进入下一波
不能继续征兵
不能继续拖拽、交换、合成
再次点击开始战斗会提示本局已失败
```

本步不做：

```text
失败结算页
重新开始按钮
复活
广告
复杂扣血规则
数值平衡
```

验收标准：

```text
点击开始战斗
敌人走到终点后生命减少
生命从 3 扣到 0 后显示失败提示
失败后不再生成敌人
失败后不再自动进入下一波
失败后不能继续征兵或拖拽单位
```

## 下一步

```text
Step 10：Expand System
使用铲子解锁红色扩展格，让扩地参与征兵、合成、战斗循环
```

## Step 10：Expand System 最小扩地

已完成。

当前规则：

```text
红色锁定格显示“铲20”
点击红色格或格中文字可尝试扩地
扩地消耗包子 20
包子不足时提示不能扩地
扩地成功后红格变白格
新白格立刻可放兵
新白格可参与交换、合成、自动攻击
```

失败后：

```text
不能继续扩地
```

本步不做：

```text
铲子道具数量
解锁动画
粒子特效
扩地层级限制
相邻格校验
扩地价格成长
```

验收标准：

```text
点击红色格
包子减少 20
红色格变成白色“空”格
把缓存区单位拖到新白格
单位能停在新白格上
新白格上的单位能自动攻击敌人
失败后点击红格会提示不能继续扩地
```

## 下一步

```text
Step 11：P0 Balance Pass
调整包子、征兵、扩地、敌人血量与波次，让玩家能连续玩 10 分钟
```

## Step 11：P0 Balance Pass 最小数值整理

已完成。

当前数值：

```text
初始包子 = 60
征兵消耗 = 10
扩地首块消耗 = 20
每扩 1 格，后续扩地价格 +5
击杀敌人奖励 = 包子 +6
清空波次奖励 = 包子 +12
第1波敌人数 = 3
每波敌人数 +1
敌人初始 HP = 18
每波敌人 HP +4
敌人初始速度 = 86
每波敌人速度 +2
```

调整目的：

```text
开局能连续征兵，也能选择先扩地
击杀奖励足够支撑继续征兵
清波奖励推动下一轮扩地
扩地价格递增，避免一口气铺满
敌人随波次变硬变快，推动合成和扩地
```

本步不做：

```text
20波正式曲线
不同敌人类型
不同兵种数值差异
完整经济表
关卡配置文件
```

验收标准：

```text
开局包子显示 60
征兵按钮显示消耗 10
第一块红格显示铲20
扩一块地后剩余红格显示铲25
第1波生成3个敌人
第2波生成4个敌人
击杀敌人获得6包子
清波获得12包子
```

## 下一步

```text
Step 12：Run Test Notes
在 Cocos 中跑 3~5 分钟，记录卡点：包子不足、敌人漏太快、扩地是否值得
```

## Step 12：Run Test Notes 跑测观察面板

已完成。

当前支持：

```text
顶部显示跑测指标
击杀数量
漏怪数量
扩地次数
当前布阵位数量
```

用途：

```text
判断输出是否足够
判断漏怪是否过早
判断扩地是否被玩家使用
判断扩地后布阵位是否正确增加
```

建议跑测记录：

```text
第几波开始漏怪？
第几波生命归零？
包子主要花在征兵还是扩地？
扩地后是否明显变强？
缓存区是否经常满？
合成是否发生得太少？
```

本步不做：

```text
自动日志文件
CSV 导出
正式数据埋点
结算统计页
可视化曲线
```

验收标准：

```text
进入游戏后顶部显示击杀/漏怪/扩地/布阵位
击杀敌人后击杀数增加
敌人到达终点后漏怪数增加
扩地成功后扩地数增加
扩地成功后布阵位增加
```

## 下一步

```text
Step 13：Cache Quality
优化缓存区体验：缓存满提示、可删除/整理低级兵，避免玩家卡死
```

## Step 13：Cache Quality 最小遣散

已完成。

当前支持：

```text
底部新增“遣散”按钮
遣散单位返还包子 +2
优先遣散缓存区最后一个单位
缓存区没有单位时，遣散棋盘上最低等级单位
遣散后释放对应缓存格或布阵格
缓存满时提示“拖到白格或遣散低级兵”
```

用途：

```text
缓存区满了不再卡死
玩家可以处理不想要的低级兵
包子不足时可以少量回血
给后续“整理/出售/升职”系统预留入口
```

本步不做：

```text
手动选择要遣散的单位
出售确认弹窗
按兵种筛选
自动整理缓存
高级单位保护
正式出售价格表
```

验收标准：

```text
点击征兵直到缓存区满
再次征兵提示可拖到白格或遣散
点击遣散后缓存区最后一个单位消失
包子增加 2
缓存区为空时点击遣散，会移除棋盘上最低等级单位
失败后不能继续遣散
```

## 下一步

```text
Step 14：Manual Run Fixes
根据 Cocos 实机跑测结果，修掉交互、数值或报错中的最高优先级问题
```

## Step 14：Manual Run Fixes 战斗手感修正

已完成。

本次修正：

```text
攻击敌人时飘出伤害数字
伤害数字按兵种颜色显示
不同兵种拥有不同伤害、射程、攻速
地图改为更简单的中心 3x2 已解锁区域
外圈保留少量待解锁区域
敌人路线改为红色外绕路线
路线结构更短、更清晰
地图格子再次放大
缓存格放大
单位方块放大
敌人方块放大
```

当前兵种差异：

```text
刀：红色，高伤害，中近射程，中等攻速
弓：蓝色，低伤害，远射程，较慢攻速
枪：绿色，中伤害，中远射程，中等攻速
骑：黄色，中高伤害，近中射程，较快攻速
```

验收标准：

```text
单位攻击时敌人上方出现 -伤害 数字
刀/弓/枪/骑飘字颜色不同
不同兵种打出的数字不同
中心只有 3x2 白格，玩法更简单
外围红紫格是待解锁范围
红色格路线从左下进入，绕外圈到右下终点
白格、红格、单位、敌人都更大更好点
```

## 下一步

```text
Step 15：Visual Hit Feedback
补充敌人受击闪烁、单位攻击闪烁，进一步增强战斗反馈
```

## Step 15：Shop Refresh + Shovel 刷新与铲子

已完成。

当前规则：

```text
初始包子 = 10
包子只用于刷新
不能用包子直接买棋子
不能用包子直接解锁紫色格
底部按钮改为刷新
刷新初始价格 = 10
第一次刷新刚好花光初始包子
每次刷新会清空底部缓存区
每次刷新一次生成 5 个候选
候选随机为 刀 / 弓 / 枪 / 骑 / 铲
刷新价格每次乘 1.2 后向上取整
```

铲子规则：

```text
铲子为特殊颜色方块
铲子写“铲”
铲子不能放到白格
铲子不能攻击
铲子不能合成
把铲子拖到紫色待解锁格
紫色格变白格
铲子消失
新白格可放塔丕
```

遣散规则：

```text
遣散只删除物品
遣散不返还包子
```

地图规则调整：

```text
中心白格保持简单
紫色待解锁格铺满主要站位
避免后期棋子不够放
点击紫色格只提示需要拖铲子
不能用包子直接解锁
```

验收标准：

```text
开局包子显示 10
点击刷新后底部出现 5 个候选
包子变成 0
刷新按钮价格变成 12
候选里可能出现铲子
拖铲子到紫色格后，紫色格变白
拖兵到新白格可以正常放置
铲子不能放到白格或参与战斗
遣散不会增加包子
```

## 下一步

```text
Step 16：Shop Odds + First Refresh
调整刷新概率，并考虑开局自动刷新一次，减少玩家第一步成本
```

## Step 16：Shop Quality + Pause 刷新保护与暂停

已完成。

本次修正：

```text
刷新不会清掉底部已有棋子
刷新只填充底部空格
底部满时不能刷新，并提示先拖走、合成或遣散
棋子可以在任意位置与同类同级合成
不要求拖到棋盘上的棋子
铲子变为稀有物品
铲子概率从 1/5 降到 1/16
增加暂停按钮
暂停后停止刷怪、敌人移动、单位攻击、波次结算
再次点击继续
```

验收标准：

```text
底部已有棋子时点击刷新，已有棋子不会消失
只会把空的底部格补满
底部5格满时点击刷新不会扣包子
拖动任意同类同级棋子松手即可合成
连续刷新时铲子明显更少见
点击暂停后战斗停止
点击继续后战斗恢复
```

## 下一步

```text
Step 17：Combat Readability
清理战斗和地图视觉，让原型更干净
```

## Step 17：Combat Readability 战斗与地图视觉清理

已完成。

本次调整：

```text
去掉弓/枪/骑/刀的临时攻击字效和线效
攻击反馈暂时只保留彩色伤害数字
红色路线改为连续道路
路线格不再显示“路”
紫色待解锁格不再显示“铲”
去掉地图内说明文字
去掉顶部整排和底部深处的多余待解锁站位
修复点击已放置棋子会误触发合成/移动导致消失的问题
合成时飘“合成 LvX”
漏怪时飘“漏怪 -1生命”
波次完成时飘“波次完成 +包子”
```

规则不变：

```text
实际伤害仍按兵种伤害 × 等级结算
不同兵种仍有不同伤害、范围、攻速和伤害数字颜色
铲子仍然通过拖到紫色待解锁格解锁白格
暂停后不会继续攻击或刷怪
```

验收标准：

```text
把不同兵种拖到白格
点击开始战斗
敌人身上仍会飘对应颜色伤害数字
红色路线看起来是一条连续道路，不再是一串写字格子
紫色待解锁格没有“铲”字
地图中间不再有说明文字遮挡
外圈待解锁站位更少、更靠近核心布阵区
点击已放置棋子不会消失
合成、漏怪、波次完成时都有额外文字反馈
```

## 下一步

```text
Step 18：Restart + Wave Pace
失败后可重开，并整理波次节奏
```

## Step 18：Restart + Wave Pace 重开与波次节奏

已完成。

本次新增：

```text
顶部初始状态显示“准备中”
失败后开始战斗按钮变为“重新开始”
点击“重新开始”会重置本局
重置后恢复初始包子、生命、波次、缓存、地图和统计
战斗中开始按钮显示“战斗中”
波次清空后不再立刻进入下一波
波次之间增加 2 秒准备时间
准备时间内按钮显示“准备中”
暂停时准备倒计时也会暂停
```

验收标准：

```text
进入游戏顶部显示“准备中”
点击开始战斗后按钮变为“战斗中”
清完一波后顶部显示准备下一波，并等待 2 秒再刷怪
生命归零后顶部显示“失败”
失败后按钮显示“重新开始”
点击重新开始后回到新一局初始状态
```

## 下一步

```text
Step 19：Tactical Route
路线穿过塔丕中间，让射程和后续特效真正有意义
```

## Step 19：Tactical Route 战术路线调整

已完成。

本次调整：

```text
路线不再走最外围
路线从布阵核心区中间穿过
初始白格改为上下两排夹住路线
敌人会经过塔丕射程核心
紫色待解锁格保留在上下外侧和左右侧
```

设计目的：

```text
让刀、枪、骑这种短中程兵种更容易参与战斗
让弓的远射程仍有优势但不再唯一有效
后续如果加弹道或攻击特效，表现会发生在画面中心
玩家布阵决策更清楚：上排/下排夹击路线
```

验收标准：

```text
敌人不再沿最外围绕圈
敌人会从上下两排白格之间经过
白格不压在红色路线之上
塔丕放在白格后能更稳定打到敌人
```

## 下一步

```text
Step 20：Playable Look Pass
不接资源，先用程序化绘制把调试画面换成可玩棋盘
```

## Step 20：Playable Look Pass 程序化视觉换皮

已完成。

本次调整：

```text
整体从粉色调试底改成暗战场色系
顶部栏、底部栏、按钮统一描边和颜色
路线改成深色描边 + 土红内路
白格改成棋盘坑位，不再显示“空”
紫格改成暗色待解锁地块
缓存格改成营帐牌槽风格
棋子从纯色块改成深框 + 彩色内牌 + 兵种大字
敌人从黑方块改成敌军牌
入口、终点、统计和提示颜色统一
修复刷新按钮价格文本节点名，刷新后价格会正常更新
```

规则不变：

```text
不接贴图资源
不改核心玩法
不改兵种数值
不改刷新、合成、铲地、波次和重开逻辑
```

验收标准：

```text
进入游戏后不再像粉色调试格子
路线、格子、棋子、敌人能明显区分
棋子比格子更突出
刷新后按钮价格文本会更新
玩法操作保持不变
```

## 下一步

```text
Step 21：Cavalry Sweep
骑兵改成挥枪范围攻击，形成明显兵种差异
```

## Step 21：Cavalry Sweep 骑兵范围挥枪

已完成。

本次调整：

```text
骑兵不再只是单体攻击
骑兵攻击时会以骑兵自身为中心横扫
骑兵攻击范围内的敌人都会受到骑兵伤害
骑兵攻击时显示一根金色长枪绕自身旋转
旋转半径跟骑兵攻击范围一致
转枪持续很短，只作为命中反馈
```

规则说明：

```text
骑兵伤害仍按 骑兵基础伤害 × 等级 结算
范围内每个敌人各吃一次伤害
刀、弓、枪暂时仍保持单体攻击
```

验收标准：

```text
多个敌人靠近时，骑兵一次攻击能同时打多个敌人
骑兵攻击时能看到金色长枪围着骑兵转一圈
其他兵种攻击逻辑不变
```

## 下一步

```text
Step 22：Clean Combat VFX
补齐刀、弓、枪的克制攻击反馈
```

## Step 22：Clean Combat VFX 干净攻击反馈

已完成。

本次新增：

```text
弓：曾尝试淡蓝短箭线，后续因激光感移除
枪：曾尝试绿色穿刺线，后续因激光感移除
刀：曾尝试短红色斩痕，后续因激光感移除
敌人受击时轻微缩放反馈
骑兵保留围绕自身转枪的范围攻击
```

规则不变：

```text
攻击特效只做反馈，后续保留克制版本
伤害仍然即时结算
刀、弓、枪仍是单体攻击
骑兵仍是自身范围攻击
```

验收标准：

```text
敌人受击能看出反馈
特效不使用文字
特效持续时间短，不遮挡棋盘
敌人受击能看出被命中
```

## 下一步

```text
Step 23：Board Coordinate Layout
改成 8×5 棋盘，并按指定坐标重建怪物路线和初始站位
```

## Step 23：Board Coordinate Layout 8×5 坐标棋盘

已完成。

本次调整：

```text
棋盘改成 40 个格子
横向 8 格，纵向 5 格
左下角为 (0,0)
所有非初始站位都是待解锁暗格
初始白格为两排 6 个：
(2,1) (3,1) (4,1)
(2,2) (3,2) (4,2)
```

路线调整：

```text
路线按格线坐标描述，但落到棋盘格本身
(0,0) -> (0,4) -> (5,4) -> (5,5) -> (8,5) -> (8,0)
不再单独画一条路线线
路线经过的棋盘格直接替换成路线格
怪物沿路线格中心移动
格子按 cell center 绘制
当前路线格为：
(0,0) -> (0,1) -> (0,2) -> (0,3)
-> (1,3) -> (2,3) -> (3,3) -> (4,3)
-> (4,4) -> (5,4) -> (6,4) -> (7,4)
-> (7,3) -> (7,2) -> (7,1) -> (7,0)
```

特效调整：

```text
去掉刀、弓、枪的线性攻击特效
避免看起来像激光
保留骑兵围绕自身转枪
保留伤害数字和敌人受击缩放
```

验收标准：

```text
棋盘能看出 8×5 共 40 格
初始只有指定两排 6 个白格
怪物按路线格中心行走
路线是棋盘格本身，不是额外覆盖线条
刀、弓、枪不再出现激光线
```

## 下一步

```text
Step 24：Polish VFX
先补非攻击类轻量特效，让画面更有反馈
```

## Step 24：Polish VFX 轻量反馈特效

已完成。

本次新增：

```text
刷新出来的新棋子会弹入
棋子放到白格时会轻微弹一下
合成成功时目标棋子放大回弹
合成成功时出现一圈短暂光点
敌人死亡时出现小碎片
```

保留：

```text
骑兵围绕自身转枪范围攻击
伤害数字
敌人受击缩放
```

暂不做：

```text
刀、弓、枪攻击线
避免再次出现激光感
```

验收标准：

```text
刷新、放置、合成、击杀都有轻量视觉反馈
特效不遮挡棋盘
操作规则不变
```

## 下一步

```text
Step 25：Unit Identity
明确兵种定位，枪做有限穿透但不做无限穿线
```

## Step 25：Unit Identity 枪兵有限穿透

已完成。

本次新增：

```text
枪兵从纯单体改为有限穿透
枪兵从目标后继续穿透 2 格距离
穿透判定宽度较窄，避免太强
枪兵不画攻击线，避免激光感
骑兵继续保持自身范围转枪
刀、弓暂时保持单体
伤害调整为 刀 9、枪 8、骑 7
```

规则说明：

```text
枪兵先锁定最近敌人
再沿枪兵到目标的方向寻找同方向敌人
只有在目标后 2 格距离内、窄带内、且仍在枪兵射程内的敌人才会被穿透
不再按人数上限截断
```

验收标准：

```text
敌人在目标后 2 格内排成线时，枪兵能穿透命中
敌人分散时，枪兵仍基本是单体
枪兵不会一条线打穿过远敌人
```

## 下一步

```text
Step 26：Enemy Types
增加基础敌人类型，让兵种定位有发挥空间
```

## Step 26：Enemy Types 基础敌人类型

已完成。

本次新增：

```text
兵：普通敌人，标准血量和速度
盾：高血低速敌人
轻：低血高速敌人
```

刷怪规则：

```text
第1波只出兵
第2波开始可能出轻
第3波开始可能出盾
第5波后轻和盾权重增加
```

数值规则：

```text
兵：HP ×1.00，速度 ×1.00
盾：HP ×1.65，速度 ×0.72
轻：HP ×0.72，速度 ×1.35
```

验收标准：

```text
场上能看到 兵 / 盾 / 轻 三种敌人
盾更耐打但移动慢
轻血少但更容易漏
不同敌人颜色不同
```

## 下一步

```text
Step 27：Merge Target Fix
修复合成自动找子和升级位置错乱
```

## Step 27：Merge Target Fix 合成落点修正

已完成。

本次修正：

```text
取消全局自动找同类同级合成
棋子只会和实际拖到的目标格棋子合成
目标格为空时正常放置
目标格有不同棋子时交换
合成后升级保留在目标格棋子身上
拖拽来源棋子消失
```

修复问题：

```text
拖动时不会自动找远处棋子合成
高等级棋子不会因为全局合成跑到错误位置
不会出现合成后像换回低级棋子的错觉
```

验收标准：

```text
拖到同类同级棋子所在格才会合成
拖到其他位置不会自动找棋子合成
合成后的高等级棋子留在被拖到的目标格
```

## 下一步

```text
Step 28：Cache Merge
底部候选区也允许落点合成
```

## Step 28：Cache Merge 底部合成修复

已完成。

本次修正：

```text
底部缓存格也可以作为拖拽落点
底部同类同级棋子拖到一起会合成
底部不同棋子拖到一起会交换
底部空格可以重新放入棋子
白格仍然是战斗站位
缓存格里的棋子不攻击
铲子逻辑不变，只能拖到暗格解锁
```

验收标准：

```text
刚刷新出来的一排棋子可以互相合成
不需要先放到战斗白格再合成
底部合成后的高等级棋子留在目标缓存格
```

## Step 29：Self Merge Guard 自合成保护

已完成。

本次修正：

```text
拖动距离不够、落回原格时不触发合成
目标格如果还是自己，直接返回原位
合成函数禁止自己和自己合成
避免自己合成后节点被销毁导致棋子消失
```

验收标准：

```text
轻微拖动棋子后松手，棋子不会消失
拖回原格不会触发合成
只有拖到另一个同类同级棋子所在格才会合成
```

## 下一步

```text
Step 30：Run Summary
移除廉价波次预告，增加失败结算
```

## Step 30：Run Summary 失败结算

已完成。

本次调整：

```text
移除每波开始的敌人类型预告
移除轻/盾首次出现的廉价提示
保留简洁的第N波开始提示
保留简洁的波次清空提示
失败时飘“本局结束”
失败后底部显示本局结算
```

结算内容：

```text
撑到第几波
击杀数量
漏怪数量
扩地数量
最高塔丕等级
```

验收标准：

```text
波次提示不再显示敌人构成
失败后能看到本局表现
重新开始后结算状态清空
```

## 下一步

```text
Step 31：Debug Panel
增加局内调试开关，方便跑测数值
```

## Step 31：Debug Panel 跑测调试面板

已完成。

本次新增：

```text
底部增加“调试”按钮
调试面板默认隐藏
点击“调试”后显示/隐藏面板
面板显示当前波数、敌人基础HP/速度、本波剩余、刷新价格、各兵种数量
```

调试按钮：

```text
+10包子
下一波
清怪
掉1血
```

用途：

```text
快速测试经济
快速跳波看强度
快速清理场上敌人
快速触发失败结算
```

验收标准：

```text
正常玩法默认不显示调试面板
点击调试后能看到当前跑测数据
调试按钮能即时生效
再次点击调试能隐藏面板
```

## 下一步

```text
Step 32：UI Layout Polish
减少空白、放大棋盘、整理底部操作区
```

## Step 32：UI Layout Polish 布局美化

已完成。

本次调整：

```text
地图层上移
地图容器宽度微调到 700，避免放大棋盘擦边
地图容器高度从 760 进一步压缩到 610
棋盘格从 76 放大到 80
棋盘间距从 82 放大到 86
明显减少地图上下空场
底部营地区块重新排版
5个候选格整体居中
底部营地整体上移
三个主按钮高度压缩并重新对齐
调试按钮缩小并挪到右下
棋盘格描边变细
棋盘格内层缩小，减少厚按钮感
```

验收标准：

```text
棋盘在地图区域里更居中、更大
底部按钮不再显得松散
调试按钮不抢主操作
整体比上一版更像游戏界面
```

## Step 33：Refresh Shop Replace 刷新替换候选

已完成。

本次修正：

```text
点击刷新时会先清掉底部候选区已有棋子
然后重新生成 5 个候选
已放到棋盘白格上的塔丕不会被清掉
刷新提示改为“重置 5 个候选”
```

验收标准：

```text
底部已有候选时点击刷新，旧候选会消失
底部刷新后始终出现 5 个新候选
棋盘上的塔丕不受刷新影响
```
